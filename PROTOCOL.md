# W4EX / CT7601 ISP プロトコル監査メモ

## 結論

転送形式はL&PのWindowsツールの静的解析と、公開されている更新デモの挙動に整合する。2026-09-05にW4EX実機1台で、消去確認、全ページ書込み、全域照合、再接続後の本体更新と通常起動に成功した。

旧実装には、SPI status `0x05` のWIP待ち不足と、status register書込み後のchip erase直前にWrite Enable `0x06` がない問題があった。現実装ではWIP待ち、消去直前のWrite Enable、消去後のblank確認、書込み後の全域照合を実装している。

## 確認済みの事実

接続中の実機は次のように列挙される。

| 項目 | 値 |
|---|---|
| VID | `2FC6` |
| PID | `F82D` |
| 製品記述 | `LuxuryPrecision UAC Device` |
| JEDEC ID | `85 60 15` |
| USB | High-Speed 480 Mbps |

この記述だけでは W4 と W4EX を区別できない。`F82D` は W4 系で使われる通常 UAC 側の識別子と考えられるが、W4EX 専用であることは確認できていない。

## ISP 移行

Windows ツールには次の要求が現れる。

```text
bmRequestType = 43
bRequest      = 84
wValue        = 5354
wIndex        = 2080
```

その直後に、同じハンドルへ次のアーム処理を送る。

| bmRequestType | bRequest | wValue | wIndex | データ |
|---:|---:|---:|---:|---|
| `43` | `90` | `0` | `0` | `[0D]` |
| `43` | `90` | `0` | `7` | `[10]` |
| `43` | `90` | `0` | `1` | `[03]` |

旧実装はこの後にハンドルを閉じ、`PID=6000` の再列挙を待っていた。静的解析では Windows の更新経路が通常デバイスを開いた同じオブジェクト／ハンドルで JEDEC 読み出しと更新を続けているため、現実装は同じハンドル方式に改めた。`PID=6000` は別経路の候補であり、現実機の更新成功条件ではない。

物理ボタンを押す手順は確認されていない。W4EX に物理ボタンがないというユーザーの観察と、公開デモの手順は一致する。

## 制御転送

macOS では Windows の raw USB 経路に対応する `IOUSBDevRequest` を使う。`bmRequestType=0x43` は OUT、`0xC3` は IN である。

| bmRequestType | bRequest | wValue | wIndex | データ |
|---:|---:|---:|---:|---|
| `43` | `80` | `0` | `0` | `[02, addr_hi, addr_mid, addr_lo]` |
| `43` | `80` | **送信長** | `1` | ページデータ |
| `43` | `81` | 読み出し長 | `1` | `[03, addr_hi, addr_mid, addr_lo]` |
| `C3` | `82` | `0` | `0` | 読み出し結果 |
| `C3` | `83` | `0` | `0` | CT7601 の転送完了フラグ |

ページサイズは JEDEC ID が `C2 22 xx` の場合だけ `0x20`、それ以外は `0x100` とする Windows ツールの分岐を採用する。今回の `85 60 15` では `0x100` である。

## SPI 操作と待機

SPI 命令は `43/80` の `wIndex=1` で `[command, payload...]` として送る。

| SPI 命令 | 意味 |
|---|---|
| `9F` | JEDEC ID |
| `05` | status register 読み出し |
| `06` | Write Enable |
| `01 00` | status register 書き込み |
| `C7` | chip erase |
| `02 + addr24` | page program のアドレス |
| `03 + addr24` | flash read |

`0x83` のフラグは CT7601 が直前の USB 要求を処理できる状態かを示す。これはフラッシュの消去・書き込み完了とは別である。SPI status `0x05` の bit0（WIP）が `0` になるまで、消去後および各ページの前後で待つ必要がある。

更新手順は次のとおりである。

1. JEDEC ID を読み、ページサイズを決める。
2. `06`、`01 00` を送り、status の WIP が解除されるまで待つ。
3. `06`、`C7` を送り、CT7601 転送完了と SPI status の WIP 解除を待つ。
4. 対象範囲を読み、全バイトが `FF` であることを確認する。失敗したら書き込まない。
5. 各ページについて、WIP 解除を確認し、`06`、アドレス、ページ長を `wValue` にしたデータ転送を行う。
6. CT7601 転送完了と WIP 解除を待つ。
7. 64 バイト単位で全対象データを読み戻し、ファイルと完全一致させる。

OUT の IOKit 成功だけでは書き込み成功と判定しない。IN は `wLenDone` が要求長と一致しなければエラーとする。

## ファームウェア適合性の問題

L&P 公式ダウンロードページは `V1.0.2.4 AS` を「小尾巴 W4 系列」として掲載している。一方、公式サイトには `W4 EX` の製品ページが別にある。現時点で、同ページの表記だけから `W4_V1.0.2.4_AS.bin` が W4EX に適合すると断定できない。ユーザー提供ファイルの SHA-256 は記録したが、公式配布物との暗号学的な照合値は取得できていない。

したがって、更新前に必要な確認は次の順序とする。

1. 本体・購入記録・画面表示で対象が W4EX であることを確認する。
2. L&P サポートに `W4_V1.0.2.4_AS.bin` が W4EX 対応か確認する。
3. 可能なら公式配布アーカイブから再取得し、ハッシュを記録する。
4. Mac ツールは VID/PID だけで機種適合と判定せず、適合性が未確認なら更新を停止する。

現実装の `update` はこの不確実性を明示するため、`--model W4EX` と `--accept-unverified-w4ex-firmware` の両方を要求する。前者はユーザーによる機種指定、後者は未確認ファイルを使うことの明示であり、どちらも公式な適合性証明ではない。

## 復旧と未確認事項

公式ガイドには、更新失敗で認識されなくなった場合にパッケージ内の `rescue_driver` を使い、Windows ツールで再試行する手順がある。現実装には Windows の rescue driver／強制 100・101・102 モードは含まれない。

未確認のまま残る点は、W4EX 専用のファイル形式・署名・容量、Mac IOKit と AppleUSBAudio の全環境での占有挙動、公式 Windows ツールが機種ごとに行う追加処理である。これらを確認するまで、別の `.bin` や `PID=6000` を使った試行を増やしてはならない。
